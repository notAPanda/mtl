<div class="col-xs-12 col-md-4">
    <div class="card">
        <div class="card-block">
            <h4 class="card-title">{{title}}</h4>
            <p class="card-text">{{description}}</p>
            <p class="card-text">{{# each tags}}<span class="label label-default">{{this}}</span> {{/each}}</p>

            <a class="btn btn-secondary btn-sm" href="{{url}}/?ref=http://www.matketingtoolslist.com" target="_blank">Check it out!</a>
        </div>
    </div>
</div>